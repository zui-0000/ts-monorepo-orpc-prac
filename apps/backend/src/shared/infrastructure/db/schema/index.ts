/**
 * DB スキーマの目次。**この DB に何があるかはここを見れば分かる** (設計関連/ADR-10)。
 *
 * 置き場所を共有基盤にまとめているが、**それは「誰でも書いてよい」を意味しない。**
 * 書き込みの境界は次の 3 つが守る。
 *
 * - DB の名前空間 (`auth` は better-auth の所有物 / 設計関連/ADR-09)
 * - コンテキストの公開ポート
 * - 「自前のコマンドは `auth` スキーマを書かない」というルール
 *
 * 集約・リポジトリのポート・その実装は、引き続き所有するコンテキストに置く。
 * ここに集めるのは**物理的なテーブル定義だけ**である。
 */

export * from "./auth.ts";
export * from "./user-profile.ts";
